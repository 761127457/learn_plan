1、volatile关键字（编译器优化之后，如果两次读取变量之间未进行写操作，则直接从缓存去读取上次读取的数据，而加了volatile关键字之后，每次都是从内存中读取变量数据，防止多线程程序中，变量被其它线程修改后无法感知，读取错误的数据）
2、定位new表达式（new(address) type[size]{braced initializer list};）
3、如何定义一个只能在堆上（栈上）生成对象的类？（方法：将析构函数设置为私有；将 new 和 delete 重载为私有）